@Library('mylibrary')
import com.cleverbuilder.GlobalVars

pipeline {
    agent any
    stages {
        stage('Demo') {
            steps {
                echo 'Hello, world'
                sayHello 'Dave'

                echo 'The value of foo is : ' + GlobalVars.foo
            }
        }
    }
}
